input_layer_config:
    layer_path: encoder.model.stem_conv1.conv
    stride_repl_layer_path: encoder.model.stages_0.conv_down.conv
    params:
        padding:
        - 1
        - 1
        stride:
        - 1
        - 1
    replace_type: channels+stride
    weight_update_type: average_all
model_params:
    encoder_name: tu-cspdarknet53
    encoder_weights: imagenet